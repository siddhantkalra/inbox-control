# Changelog

## [Unreleased]
- Engine CLI: scan, suppress, list-suppressed, undo, detect.
- Gmail OAuth + API integration (readonly + modify).
- Suppression journaling for undo support.
- Undo can optionally delete empty suppression labels.
- Detect command outputs ranked bulk candidates with explainable signals.

